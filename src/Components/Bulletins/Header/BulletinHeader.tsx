import React from "react"; //importing the library?

const Header: React.FC = () => {
  return <h1 className="ui dividing centered header">BULLETINS</h1>;
}; //creating a function called Header that returns a header that says BULLETINS
export default Header; //exporting Header so it can be used elsewhere
